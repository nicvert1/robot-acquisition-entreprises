services:
  - type: web
    name: robot-acquisition
    env: python
    region: oregon
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python robot.py"
    autoDeploy: true
